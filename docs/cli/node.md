---
summary: "CLI reference for `openclaw node` (node-specific commands)"
read_when:
  - You are managing a single node identity
title: "node"
---

# `openclaw node`

Node identity and connection management.

```bash
openclaw node status
openclaw node connect
openclaw node disconnect
openclaw node info
```
