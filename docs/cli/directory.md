---
summary: "CLI reference for `openclaw directory` (show key file paths)"
read_when:
  - You need to find where config/state files are stored
title: "directory"
---

# `openclaw directory`

Show key directory paths for config, state, and logs.

```bash
openclaw directory
openclaw directory --json
```
